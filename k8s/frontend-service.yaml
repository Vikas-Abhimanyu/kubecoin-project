apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend
  namespace: testing
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 81
      targetPort: 8080
      nodePort: 30001
