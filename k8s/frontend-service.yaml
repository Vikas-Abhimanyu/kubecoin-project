apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend
  namespace: production
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 82
      targetPort: 8080
      nodePort: 30002
