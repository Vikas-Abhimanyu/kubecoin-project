apiVersion: v1
kind: ResourceQuota
metadata: 
  name: dev-quota
  namespace: dev
spec:
  hard:
    pods: "6"
    requests.cpu: "500m"
    limits.cpu: "1"
    requests.memory: 512Mi
    limits.memory: 1Gi
