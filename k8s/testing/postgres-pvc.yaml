apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: postgres-pvc
  labels:
    app: postgres
  namespace: testing
spec:
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
