apiVersion: v1
kind: Service
metadata: 
  name: backend-service
  labels:
    app: backend
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
