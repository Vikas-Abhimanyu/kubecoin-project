apiVersion: v1
kind: ConfigMap
metadata: 
  name: kubecoin-config
  labels:
    app: kubecoin
  namespace: production
data: 
  POSTGRES_DB: "kubecoin"
  DB_HOST: "postgres-headless"
  DB_NAME: "kubecoin"
  BACKEND_URL: "http://backend-service.production.svc.cluster.local:5000"
