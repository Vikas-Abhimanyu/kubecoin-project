apiVersion: v1
kind: PersistentVolume
metadata: 
  name: postgres-pv-testing
  labels:
    app: postgres
    env: testing
spec:
  capacity:
    storage: 1Gi
  accessModes: 
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath: 
    path: /data/postgres/testing1
