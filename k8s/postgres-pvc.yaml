apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-testing
  namespace: testing
spec:
  volumeName: postgres-pv-testing  
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""

